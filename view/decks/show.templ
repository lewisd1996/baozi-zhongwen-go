package decks

import (
	"github.com/lewisd1996/baozi-zhongwen/view/layout"
)

templ Show(route string) {
	@layout.Base(route, true) {
		<div class="flex flex-1 flex-col flex-wrap">
			<h1 class="text-slate-900 font-semibold text-lg lg:hidden">Decks</h1>
			@DecksList("")
		</div>
	}
}
